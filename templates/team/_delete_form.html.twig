<form id="team-delete" method="post" action="{{ path('app_team_delete', { id: team.id }) }}" onsubmit="return confirm('{{ 'common.ask_confirmation'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}">
</form>
