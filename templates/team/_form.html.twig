{{ form_start(form, {'attr': {'id': 'team-form'}}) }}
    <div class="row justify-content-center">
        <div class="col-md-8 col-sm-10 col-12">
            <h3>{{ 'view.team.title'|trans }}</h3>
            {{ form_row(form.name) }}
            {{ form_row(form.members) }}
            {{ form_row(form.parentTeam) }}
        </div>
    </div>
{{ form_end(form) }}
