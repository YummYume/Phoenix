{{ form_start(form, {'attr': {'id': 'portfolio-form'}}) }}
    <div class="row justify-content-center">
        <div class="col-md-8 col-sm-10 col-12">
            <h3>{{ 'view.portfolio.title'|trans }}</h3>
            {{ form_row(form.name) }}
            {{ form_row(form.projects) }}
        </div>
    </div>
{{ form_end(form) }}
